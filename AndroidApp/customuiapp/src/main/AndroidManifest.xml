<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jings.ex.android.com.customuiapp">

  <application
      android:name="jings.ex.android.com.customuiapp.suspensiondemo.MyApp"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".MainActivity">

    </activity>

    <activity android:name=".drawable.SelfDrawableActivity">

    </activity>

    <activity android:name=".suspensiondemo.MainActivity">

    </activity>

    <activity android:name=".bigimage.BigImageActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>